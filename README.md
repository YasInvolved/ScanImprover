# Scan Improver
Python script that turns gray(-ish) pixels into white and makes document scans look like they were intact.

# Usage
`python3 scanimprover.py {path_to_the_document}`
Result is stored in directory where input document is stored. The filename pattern is `{input_doc_name}_improved.{extension}`